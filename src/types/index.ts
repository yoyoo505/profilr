export interface SocialLink {
  id: string;
  platform: string;
  url: string;
  icon: string;
  color: string;
}

export interface UserProfile {
  name: string;
  bio: string;
  avatar: string;
  username: string;
}
